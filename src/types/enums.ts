export enum UserTypes {
  USER = 'User',
  ORGANIZATION = 'Organization'
}

export enum LinkHeaders {
  next = 'link-next',
  first = 'link-first'
}

export enum AppStatus {
  INFO = 'info',
  DANGER = 'danger',
  WARNING = 'warning',
  SUCCESS = 'success'
}
